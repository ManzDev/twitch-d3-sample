export const width = 500;
export const height = 500;
export const REGIONS = ["Asia", "Europe", "Africa", "Oceania", "Americas"];

export const MIN_VALUE = 0;
export const MAX_VALUE = 1400000000;
// Math.max(...data.map(country => country.value));
